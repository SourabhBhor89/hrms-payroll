spring:
  application:
    name: hrms-backend

 
  datasource:
    url: jdbc:postgresql://db.xpjtliwteybzoczamafk.supabase.co:5432/
    databse: postgres
    username: postgres
    password: ${DB_PASSWORD}
    # driver-class-name: com.mysql.cj.jdbc.Driver
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    clean-disabled: false
    clean-on-validation-error: true
    ignore-migration-patterns: "*:*"

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt:
    secret: ${JWT_SECRET:bXktZGV2LWhybXMtc2VjcmV0LWtleS1tdXN0LWJlLWxvbmctZW5vdWdoLTEyMzQ1Njc4OTA=}
    expiration: ${JWT_EXPIRATION:3600000}

  admin:
    email: ${ADMIN_EMAIL:admin@hrms.local}
    password: ${ADMIN_PASSWORD:Admin@12345}

google:
  calendar:
    api-key: ${GOOGLE_CALENDAR_API_KEY:xyz_Api_Key}
    calendar-id: en.indian#holiday@group.v.calendar.google.com